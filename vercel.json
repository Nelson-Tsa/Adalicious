{
  "rewrites": [
    { "source": "/(.*)", "destination": "/browser/$1" }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/browser/(.*)", "dest": "/browser/$1" },
    { "src": "/(.*)", "dest": "/browser/index.html" }
  ]
}
